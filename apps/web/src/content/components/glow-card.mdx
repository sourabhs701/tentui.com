---
title: Glow Card
description: A pointer-reactive Glow Card for React and shadcn/ui with a spring-smoothed border highlight, reduced-motion support, and editable source code.
createdAt: 2026-07-21
updatedAt: 2026-07-29
---

<ComponentPreview name="glow-card-demo" />

Glow Card is a copy-and-paste React wrapper that adds a pointer-reactive radial
highlight to the border of any card. Use it with the shadcn/ui Card component or
your own markup, then customize the glow color and radius to match your design.

## Features

- Tracks fine-pointer movement with a lightly damped spring.
- Wraps shadcn/ui cards or custom React content without imposing a layout.
- Ignores touch pointers and removes the moving glow for reduced-motion users.
- Composes external pointer handlers without hiding the built-in response.

## Installation

<CodeTabs>

<TabsListInstallType />

<TabsContent value="cli">

```bash
npx shadcn@latest add @tentui/glow-card
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies</Step>

```bash
npm install motion clsx tailwind-merge
```

<Step>Add a cn helper</Step>

<ComponentSource
  src="src/registry/lib/utils.ts"
  title="lib/utils.ts"
  collapsible="false"
/>

<Step>Copy and paste the following code into your project</Step>

<ComponentSource name="glow-card" title="components/glow-card.tsx" />

<Step>Update the import paths to match your project setup</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { GlowCard } from "@/components/glow-card";
```

```tsx
<GlowCard glowColor="hsl(217 91% 60%)" glowSize={140}>
  <article className="rounded-[inherit] bg-card p-6">
    Your content
  </article>
</GlowCard>
```

## Accessibility

The glow is decorative and hidden from assistive technology. Touch input does
not activate pointer tracking, and reduced-motion preferences disable the moving
effect while preserving the card content and border.

## Related components

Use [Animated Arrow](/components/animated-arrow) for a smaller directional hover
effect, or read [How to Add Animations to shadcn/ui Components](/blog/how-to-add-animations-to-shadcn-components)
for practical guidance on purposeful motion, reduced-motion fallbacks, and
animation performance.

## API reference

<AutoTypeTable
  path="src/registry/components/glow-card/glow-card.tsx"
  name="GlowCardProps"
/>
