---
title: Stateful Button
description: Communicate async loading, success, and error states without leaving the button.
image: https://cdn.srb.codes/images/components/animated-save-button-light.webp
createdAt: 2026-07-19
updatedAt: 2026-07-26
---

<ComponentPreview name="stateful-button-demo" canConfigure />

Read the complete implementation guide to learn how the component models a
[shadcn loading button with success and error states](/blog/shadcn-loading-button),
prevents rapid duplicate actions, announces status, and handles reduced motion.

## Features

- Reports loading, success, and error states for any asynchronous action.
- Animates label changes character by character.
- Announces status changes and reduces motion when requested.

## Installation

<CodeTabs>

<TabsListInstallType />

<TabsContent value="cli">

```bash
npx shadcn@latest add @tentui/stateful-button
```

</TabsContent>

<TabsContent value="manual">

<Steps>

<Step>Install the following dependencies</Step>

```bash
npm install lucide-react motion clsx tailwind-merge
```

<Step>Add a cn helper</Step>

<ComponentSource
  src="src/registry/lib/utils.ts"
  title="lib/utils.ts"
  collapsible="false"
/>

<Step>Copy and paste the following code into your project</Step>

<ComponentSource
  name="stateful-button"
  title="components/stateful-button.tsx"
/>

<Step>Update the import paths to match your project setup</Step>

</Steps>

</TabsContent>

</CodeTabs>

## Usage

```tsx
import { StatefulButton } from "@/components/stateful-button";
```

```tsx
<StatefulButton
  labels={{ loading: "Saving", success: "Saved" }}
  onClick={async () => {
    await saveChanges();
  }}
>
  Save changes
</StatefulButton>
```

## Accessibility

The button disables itself while an action is running and announces loading,
success, and error labels through a polite live region. Reduced-motion preferences
remove transforms and stop the loading icon's rotation.

## API reference

<AutoTypeTable
  path="src/registry/components/stateful-button/stateful-button.tsx"
  name="StatefulButtonProps"
/>

## Examples

### Custom labels

```tsx
<StatefulButton
  labels={{
    loading: "Publishing",
    success: "Published",
    error: "Try again",
  }}
  onClick={async () => {
    await publishChanges();
  }}
>
  Publish
</StatefulButton>
```
